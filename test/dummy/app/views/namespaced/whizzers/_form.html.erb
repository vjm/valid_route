<%= form_for(@namespaced_whizzer) do |f| %>
  <% if @namespaced_whizzer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@namespaced_whizzer.errors.count, "error") %> prohibited this namespaced_whizzer from being saved:</h2>

      <ul>
      <% @namespaced_whizzer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :permalink %><br>
    <%= f.text_field :permalink %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
